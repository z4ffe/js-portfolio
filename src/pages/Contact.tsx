import {Flex} from '@chakra-ui/react'
import {pagesAnimation} from '../utils/pagesAnimation.ts'

export const Contact = () => {
	return (
		<Flex id='contact' height='100vh' {...pagesAnimation}>
			<h1>Contact</h1>
		</Flex>
	)
}